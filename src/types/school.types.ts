export interface SchoolYear {
  _id: string;
  code: string;
}

export interface EducationalSystem {
  _id: string;
  name: string;
}

export interface GradeLevel {
  _id: string;
  name: string;
  code: string;
} 